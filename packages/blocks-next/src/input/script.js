import { ScInput } from '@surecart/components/dist/components/sc-input';
if (!customElements.get(ScInput)) {
	customElements.define('sc-input', ScInput);
}
import { ScFormControl } from '@surecart/components/dist/components/sc-form-control';
if (!customElements.get(ScFormControl)) {
	customElements.define('sc-form-control', ScFormControl);
}
