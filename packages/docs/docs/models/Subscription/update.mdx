# Update

import MethodText from '../../../components/MethodText.js';

<p><MethodText path={'subscriptions/{id}'} method={'patch'}  /></p>

## Update Attributes
import ModelBody from '../../../components/ModelBody.js';

<ModelBody path={'subscriptions/{id}'} method={'patch'} model={'subscription'} />

## Response Attributes
import ModelResponse from '../../../components/ModelResponse.js';

<ModelResponse path={'subscriptions/{id}'} method={'patch'} model={'subscription'} />
