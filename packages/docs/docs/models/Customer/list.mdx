# List or Query

import MethodText from '../../../components/MethodText.js';

<p><MethodText path={'customers'} method={'get'}  /></p>

## Examples

Get 20 customers based on a query.
```php
\SureCart\Models\Coupon::where([
	'query' => 'ted bundy'
])->paginate([
	'page' => 1,
	'per_page' => 20
]);
```

## Where Parameters
import ModelParameters from '../../../components/ModelParameters.js';

<ModelParameters path={'customers'} method={'get'} />

## Response Attributes

import ModelResponse from '../../../components/ModelResponse.js';

<ModelResponse path={'customers'} method={'get'} model={'customer'} />
