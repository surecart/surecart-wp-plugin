# Update

import MethodText from '../../../components/MethodText.js';

<p><MethodText path={'prices/{id}'} method={'patch'}  /></p>


## Examples

All at once:
```php
$price = \SureCart\Models\Price::find('8ba8d60f-5277-4e6b-807c-dee8166446d5');
$price->update([
	'recurring' => true, // recurring price
	'recurring_interval' => 'month', // monthly
	'recurring_interval_count' => 3 // every 3 months
]);
```

Wait until attributes are updated:
```php
$price = \SureCart\Models\Price::find('8ba8d60f-5277-4e6b-807c-dee8166446d5');
$price->recurring = true; // recurring price
$price->recurring_interval = 'month'; // monthly
$price->recurring_interval_count = 3; // every 3 months
$price->save();
```

## Update Attributes
import ModelBody from '../../../components/ModelBody.js';

<ModelBody path={'prices/{id}'} method={'patch'} model={'price'} />

## Response Attributes
import ModelResponse from '../../../components/ModelResponse.js';

<ModelResponse path={'prices/{id}'} method={'patch'} model={'price'} />

