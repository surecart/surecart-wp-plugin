# Create
import MethodText from '../../../components/MethodText.js';

<p><MethodText path={'prices'} method={'post'}  /></p>

## Examples

```php
$saved = new CheckoutEngine\Models\Price([
	'name' => 'Price 1',
	'amount' => 1000,
	'currency' => 'usd',
	'recurring' => true, // recurring price
	'recurring_interval' => 'month', // monthly
	'recurring_interval_count' => 3 // every 3 months
])->save();
```


## Model Attributes
import ModelBody from '../../../components/ModelBody.js';

<ModelBody path={'prices'} method={'post'} model={'price'} />

## Response Attributes

import ModelResponse from '../../../components/ModelResponse.js';

<ModelResponse path={'prices'} method={'post'} model={'price'} />
