# Find with ID
Returns a single payment_intent by id.

## Parameters
import ModelParameters from '../../../components/ModelParameters.js';

<ModelParameters path={'payment_intents/{id}'} method={'get'} />

## Response Attributes

import ModelResponse from '../../../components/ModelResponse.js';

<ModelResponse path={'payment_intents/{id}'} method={'get'} model={'payment_intent'} />
