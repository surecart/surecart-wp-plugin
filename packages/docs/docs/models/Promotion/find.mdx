# Find with ID
Returns a single promotion by id.

## Examples

```php
\CheckoutEngine\Models\Promotion::with([
	'coupon'
])->find('8ba8d60f-5277-4e6b-807c-dee8166446d5');
```

## Parameters
import ModelParameters from '../../../components/ModelParameters.js';

<ModelParameters path={'promotions/{id}'} method={'get'} />

## Response Attributes

import ModelResponse from '../../../components/ModelResponse.js';

<ModelResponse path={'promotions/{id}'} method={'get'} model={'promotion'} />
