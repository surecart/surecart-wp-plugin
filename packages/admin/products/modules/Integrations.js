import { __ } from '@wordpress/i18n';

import Box from '../../ui/Box';

export default ({ loading }) => {
	return (
		<Box loading={loading} title={__('Integrations', 'surecart')}>
			asdf
		</Box>
	);
};
